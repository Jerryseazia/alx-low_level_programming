#include "main.h"
/**
 * _islower - print only lowercase keys
 * @c: is tht int that is use for thr argument
 * Return: To 0 or 1
 */
int _islower(int c)
{
if (c > 96 && c < 123)
return (1);
else
return (0);
}
